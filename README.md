# Gemini ItemBank 查詢建構器

歡迎！此專案旨在協助您透過 AI 與 ItemBank 資料庫互動。您不需要自己撰寫程式碼，Gemini 會為您代勞。

## 🚀 快速開始

請按照以下簡單步驟設定您的環境。

### 1. 事前準備
- **Node.js**: 請確保您已安裝 Node.js (在終端機輸入 `node -v` 檢查)。

### 2. 安裝
在終端機中開啟此目錄並執行：

```bash
npm install
```

### 3. 設定
1.  複製範例設定檔：
    ```bash
    cp .env.example .env
    ```
2.  使用文字編輯器開啟 `.env` 檔案。
3.  將 `MONGODB_URI` 的值替換為您實際的 MongoDB 連線字串。
    *   *注意：切勿分享您的 `.env` 檔案或將完整的連線字串貼到聊天視窗中。*

### 4. 驗證
執行檢查腳本以確保一切運作正常：

```bash
npm run check-env
```

如果您看到 **"✅ 連線成功！"** 的訊息，表示您已準備就緒！

---

## 🤖 如何透過 Gemini 使用

設定完成後，您可以開始要求 Gemini 執行任務。

### 1. 開啟 Gemini
在此目錄中開啟聊天工作階段。

### 2. 提出問題
簡單描述您想尋找什麼或做什麼。Gemini 了解資料庫結構。

**提示範例：**
*   「找出 2024 年建立的所有『數學』科目題目。」
*   「計算有多少五欄檔案的狀態為『處理中』。」
*   「列出所有已啟用的產品名稱。」

### 3. 執行
Gemini 會為您產生腳本。您可以使用 `node` 來執行它。

**範例：**
> Gemini: "我已建立腳本 `src/find-math-items.js`。請執行以下指令："
>
> `node src/find-math-items.js`

---

## 📁 專案結構

*   **`docs/`**: 資料庫 Schema 的官方定義。
*   **`src/`**: 腳本存放位置。
    *   **`utils/db.js`**: Gemini 用來安全連線資料庫的輔助工具。
    *   **`check-env.js`**: 設定檢查工具。
*   **`.env`**: 您的私人設定檔 (包含密碼)。